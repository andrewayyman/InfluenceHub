## 1. Project Initialization

- [x] 1.1 Initialize new Vite React TypeScript project in `InfluenceHubWeb`
- [x] 1.2 Install core dependencies (`react-router-dom`, `axios`)
- [x] 1.3 Install dev dependencies (`tailwindcss`, `postcss`, `autoprefixer`)
- [x] 1.4 Initialize and configure Tailwind CSS (`tailwind.config.js`, `postcss.config.js`)
- [x] 1.5 Add Tailwind directives to `index.css` and remove default styles

## 2. Core Architecture Setup

- [x] 2.1 Create project directory structure (`components`, `pages`, `layouts`, `services`, `hooks`, `types`)
- [x] 2.2 Configure Axios instance with base URL and interceptors in `services/api.ts`
- [x] 2.3 Setup React Router in `App.tsx` with basic routes
- [x] 2.4 Create `types/index.ts` for shared types

## 3. UI Components & Layouts

- [x] 3.1 Create `components/common/Button.tsx` (reusable button)
- [x] 3.2 Create `components/common/Input.tsx` (reusable input)
- [x] 3.3 Create `components/layout/Header.tsx` (navigation header)
- [x] 3.4 Create `components/layout/Footer.tsx` (site footer)
- [x] 3.5 Create `layouts/MainLayout.tsx` wrapping Header, Outlet, and Footer

## 4. Pages & Routing

- [x] 4.1 Create `pages/Home.tsx` as a landing page placeholder
- [x] 4.2 Create `pages/NotFound.tsx` for 404 errors
- [x] 4.3 Implement `MainLayout` in `App.tsx` routes
- [x] 4.4 Verify application builds and runs without errors
